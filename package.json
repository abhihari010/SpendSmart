{
  "name": "spendsmart-starter",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n BE,FE -c blue,green \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w frontend && echo \"(backend has no build step)\"",
    "start": "npm run start -w backend",
    "postinstall": "echo \"Use 'npm install -w frontend' and 'npm install -w backend' to install per workspace if needed.\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "dependencies": {
    "@radix-ui/react-accordion": "^1.2.12",
    "class-variance-authority": "^0.7.1",
    "lucide-react": "^0.553.0",
    "tailwind-variants": "^3.1.1"
  }
}
